{
  "name": "axotbot",
  "version": "3.0.0",
  "description": "🐍🍒⛏🤖 AxotBot – CPython backport & merge bot for zrsx/cpython",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "latest"
  },
  "license": "Apache-2.0"
}
